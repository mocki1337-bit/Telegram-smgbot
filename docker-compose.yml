version: "3.8"
services:
backend:
build: ./backend
container_name: authsrv
ports:
- "8080:8080"
restart: unless-stopped


frontend:
build:
context: ./frontend
container_name: webapp
ports:
- "3000:3000"
depends_on:
- backend
restart: unless-stopped
